name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install PHP and release tooling
        run: |
          apt-get update -qq
          DEBIAN_FRONTEND=noninteractive apt-get install -y -qq php-cli zip unzip gh
          php -v | head -1
      - name: PHP syntax check
        run: |
          errors=0
          find includes -name '*.php' | while read -r f; do php -l "$f" || exit 1; done
          echo "All PHP files pass syntax check"
      - name: Version sync gate
        run: |
          TAG="${GITHUB_REF##*/}"
          VERSION="${TAG#v}"
          echo "Tag:     $TAG"
          echo "Version: $VERSION"
          # LVC-209: versions live in .version.yaml. Both the release gate and
          # the bump tool read the SAME declaration via version-sync.py. This
          # replaces the old hand-written header/const/readme grep, which would
          # not follow a new version location (e.g. a future composer.json).
          # check-tag additionally compares source_of_truth against the TAG
          # (the CAP-CI-001 case: a vX tag pointing at an older commit).
          python3 scripts/version-sync.py check-tag "${VERSION}"
      - name: Build Plugin ZIP
        run: |
          bash scripts/create-plugin-zip.sh
          ZIP=$(ls /tmp/elementeer-build-final/*/elementeer.*.zip | head -1)
          echo "ZIP=$ZIP" >> $GITHUB_ENV
      - name: Verify ZIP structure
        run: |
          # Capture the listing first: piping straight into `grep -q` makes grep
          # close the pipe on the first match, unzip dies of SIGPIPE, and under
          # pipefail (which the runner enables) that fails the step even though
          # the ZIP is correct.
          listing=$(unzip -l "$ZIP")
          if ! printf '%s\n' "$listing" | grep -q "elementeer/"; then
            echo "ZIP structure INVALID"
            exit 1
          fi
          echo "ZIP structure OK: $(unzip -l "$ZIP" | tail -1)"
      - name: Create Forgejo Release
        run: |
          TAG="${GITHUB_REF##*/}"
          VERSION="${TAG#v}"
          BODY=$(cat <<EOBODY
          ## Elementeer $VERSION

          The agent-native Elementor growth layer.

          - PHP syntax validated
          - Version sync confirmed
          - Plugin ZIP built and attached

          ### Installation
          1. Download the ZIP below
          2. Upload to WordPress -> Plugins -> Add New -> Upload
          3. Activate

          Canonical source: [git.langevc.com/elementeer/elementeer](https://git.langevc.com/elementeer/elementeer)
          EOBODY
          )
          curl -s -X POST "https://git.langevc.com/api/v1/repos/$GITHUB_REPOSITORY/releases" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg tag "$TAG" --arg name "Elementeer v$VERSION" --arg body "$BODY" '{tag_name: $tag, name: $name, body: $body}')" \
            > /tmp/release-response.json
          cat /tmp/release-response.json
          RELEASE_ID=$(jq -r '.id' /tmp/release-response.json)
          echo "Release created: id=$RELEASE_ID"
          echo "RELEASE_ID=$RELEASE_ID" >> $GITHUB_ENV
      - name: Upload Release Asset
        run: |
          ZIPNAME=$(basename "$ZIP")
          curl -s -X POST "https://git.langevc.com/api/v1/repos/$GITHUB_REPOSITORY/releases/$RELEASE_ID/assets?name=$ZIPNAME" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/zip" \
            --data-binary "@$ZIP"
          echo "Asset uploaded: $ZIPNAME"
      - name: Mirror Release to GitHub
        env:
          GH_TOKEN: ${{ secrets.GH_MIRROR_TOKEN }}
        run: |
          TAG="${GITHUB_REF##*/}"
          VERSION="${TAG#v}"
          NOTES=$(cat <<'EONOTES'
          ## Elementeer {TAG}

          The agent-native Elementor growth layer.

          - PHP syntax validated
          - Version sync confirmed
          - Plugin ZIP built and attached

          ### Installation
          1. Download the ZIP below
          2. Upload to WordPress - Plugins - Add New - Upload
          3. Activate
          EONOTES
          )
          NOTES=$(echo "$NOTES" | sed "s/{TAG}/$TAG/")
          gh release create "$TAG" \
            --repo elementeer/elementeer \
            --title "Elementeer $TAG" \
            --notes "$NOTES" \
            "$ZIP#Elementeer v$VERSION - installable WordPress plugin" \
            || echo "Mirror release skipped (token may lack permissions)"
